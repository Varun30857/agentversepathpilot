{
  "name": "pathpilot-agentverse",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev --prefix profile-intelligence-agent/frontend",
    "build": "npm run build --prefix profile-intelligence-agent/frontend"
  }
}
